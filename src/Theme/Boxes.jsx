import styled from 'styled-components';
import {
  color,
  space,
  typography,
  layout,
  flexbox,
  background,
  border,
} from 'styled-system';

export const Boxes = styled('div')(
  color,
  space,
  typography,
  layout,
  flexbox,
  background,
  border
);
